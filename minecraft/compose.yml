services:
  minecraft:
    image: itzg/minecraft-server
    tty: true
    container_name: minecraft
    stdin_open: true
    environment:
      EULA: "TRUE"
      INIT_MEMORY: 10G
      MAX_MEMORY: 40G
    volumes:
      - ./data:/data
    network_mode: host
    restart: unless-stopped
    